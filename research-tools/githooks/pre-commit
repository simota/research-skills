#!/bin/sh
# Budgets enforced by a mechanism rather than by remembering.
# Install with: make hooks
set -e
python3 "$(git rev-parse --show-toplevel)/research-tools/validate.py"
python3 "$(git rev-parse --show-toplevel)/research-tools/figures_check.py"
